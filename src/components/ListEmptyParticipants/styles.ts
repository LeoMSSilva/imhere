import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  listEmpty: {
    marginTop: 12,
    textAlign: 'center',
    color: '#fdfcfe',
    fontWeight: '400',
    fontSize: 16,
  },
});
